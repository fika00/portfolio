import React, { useContext, useEffect, useState } from "react";
import "./navbar.css";
import { connect, useSelector } from "react-redux";

const Navbar = ({ h1, p, point }) => {
  return <div className="navwrapper"></div>;
};

export default Navbar;
